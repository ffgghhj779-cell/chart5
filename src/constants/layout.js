export const LAYOUT = {
  VOL_RATIO:   0.15,   // bottom 15 % of screen height → volume panel
  PROFILE_W:   152,    // px – right-side profile histogram panel
  PRICE_AXIS:   72,    // px – price axis label area
  LEFT_PAD:      8,    // px – left padding before first candle
  TIME_AXIS:    22,    // px – bottom time axis height
  BINS:         64,    // number of histogram bins in the profile
  VA_PCT:       0.70,  // value-area target (70 % of visible volume)
  MIN_CANDLES:  10,    // minimum candles to show when zooming in
};
